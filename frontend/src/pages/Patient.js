import React from 'react';
import {Link} from "react-router-dom";

const Patient = () => {
    return (
        <div>
            Placeholder patient page. <Link to="/" className="text-indigo-600">Go back to Home Page</Link>.
        </div>
    );
};

export default Patient;
