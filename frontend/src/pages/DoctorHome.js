import React from 'react';
import DashboardLayout from "../dashboard/layout";
import SectionTitle from "../components/SectionTitle";

const DoctorHome = () => {
    return (
        <SectionTitle>Welcome Doctor</SectionTitle>
    );
};

export default DoctorHome;
